{
  "name": "nodejs-zip-selective-extract",
  "version": "1.0.0",
  "description": "This library is able to extract single files from zip files that are hosted on S3. Importantly, where (every other?) method streams the entire zip file from s3 into the compute, this library uses byte range selection to retrieve only the relevant data from the zip file.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.374.0"
  }
}
